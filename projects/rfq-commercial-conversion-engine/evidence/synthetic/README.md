# Synthetic evidence

Synthetic files are for CI, regression and demos only.

They must not be used as production evidence for:
- real customer conversion;
- classifier accuracy;
- pricing decisions;
- automation thresholds.

Every future synthetic record should contain `synthetic=true`.
