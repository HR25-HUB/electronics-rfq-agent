index: gamm_catalog_products
mode: read_only
timeout_seconds: 10
follow_redirects: false
expected_fields:
  - id
  - sku
  - name
  - brand
  - category
  - attributes
notes:
  - Do not store credentials in this file.
  - Inject base URL and authorization at runtime.
